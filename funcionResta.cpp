#include <iostream>
#include <vector> 
#include "funciones.h"

vector<int> restaVectores(vector<int> v1, vector<int> v2)
{
	if (v1.size() == v2.size())
	{
		int N;
		N = v1.size();
		for (int i = 0; i < N; i++)
		{
			resta[i] = v1[i] - v2[i];
		}
	}
	else {
		cout << "vectores de diferente tamaño"
	} 
	return resta
}